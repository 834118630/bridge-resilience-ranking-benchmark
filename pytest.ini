[pytest]
# Only the project test suite is collected. The release copy of the analysis
# package lives under paper_rewriting_output/repo_release and must not be
# collected, because its test modules share basenames with these.
testpaths = tests
norecursedirs = .git .venv venv build dist paper_rewriting_output tmp

markers =
    integration: runs commands that regenerate archived outputs
